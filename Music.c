// Music.c
// Runs on TM4C123
// this song contains APIs for needed for playing a music
// Aria Pahlavan, Mehrad Yousefi

#include <shared/Logging.h>
#include <shared/SoundArrays.h>
#include "Music.h"

/*
const unsigned short *ShapeOf(Instrument i) {
        switch (i) {
                case BASSON:
                        return bassoon64;
                case FLUTE:
                        return flute64;
                case OBOE:
                        return oboe64;
                case TRUMPET:
                        return trumpet64;
                case GUITAR:
                        return guitar64;
                case SINE:
                        return sineWave64;
                default:
                        ERROR("Invalid Instrument");
                        return 0;
        }
}*/
